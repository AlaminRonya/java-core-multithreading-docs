# OSI ৭-লেয়ার মডেল প্রোটোকল গাইড
**প্রস্তুতকারক:** Md. Al Amin (Software Engineer)  
**তারিখ:** জুন ৫, ২০২৬  

---

কম্পিউটার নেটওয়ার্কিং স্ট্যান্ডার্ড এবং আর্কিটেকচার অনুযায়ী OSI (Open Systems Interconnection) মডেলের প্রতিটি স্তরে যেসকল গুরুত্বপূর্ণ প্রোটোকল, সার্ভিস এবং টেকনোলজি কাজ করে, তা নিচে ধাপে ধাপে সুনির্দিষ্টভাবে উপস্থাপন করা হলো:

### L7: Application Layer (অ্যাপ্লিকেশন লেয়ার)
এটি ব্যবহারকারীর সবচেয়ে কাছের লেয়ার, যেখানে নেটওয়ার্ক অ্যাপ্লিকেশনগুলো রান করে।
* **HTTP / HTTPS:** ওয়েব ব্রাউজার এবং সার্ভারের মধ্যে যোগাযোগের প্রধান প্রোটোকল (যেমন: ওয়েবসাইট ভিজিট করা)।
* **FTP / SFTP:** নেটওয়ার্কে ফাইল নিরাপদভাবে আপলোড বা ডাউনলোড করার জন্য ব্যবহৃত হয়।
* **SMTP / IMAP / POP3:** ক্লায়েন্ট এবং সার্ভারের মধ্যে ইমেইল আদান-প্রদান করার স্ট্যান্ডার্ড প্রোটোকল।
* **SSH / Telnet:** রিমোটলি কোনো কম্পিউটার বা সার্ভারে কমান্ড লাইনের মাধ্যমে সুরক্ষিতভাবে লগইন করার জন্য।
* **DNS:** হিউম্যান-রিডেবল ডোমেন নেমকে (যেমন: google.com) মেশিনের আইপি অ্যাড্রেসে রূপান্তর করে।
* **DHCP:** নেটওয়ার্কের নতুন ডিভাইসগুলোকে স্বয়ংক্রিয়ভাবে আইপি অ্যাড্রেস বরাদ্দ করে।

### L6: Presentation Layer (প্রেজেন্টেশন লেয়ার)
এই লেয়ারটি ডেটার সঠিক ফরম্যাট, কম্প্রেশন (সংকোচন) এবং এনক্রিপশন নিশ্চিত করে।
* **SSL / TLS:** ডেটার নিরাপত্তা ও ক্রিপ্টোগ্রাফিক এনক্রিপশনের জন্য (যেমন: HTTPS-এর পেছনের মূল সিকিউরিটি)।
* **ASCII / UTF-8:** টেক্সট ক্যারেক্টারগুলোকে স্ট্যান্ডার্ড বাইনারি এনকোডিং ফরম্যাটে রূপান্তর করার নিয়ম।
* **JPEG / PNG / GIF:** ডিজিটাল ইমেজ বা ছবির স্ট্যান্ডার্ড ফাইল ফরম্যাট প্রোটোকল।
* **MPEG / MP4 / AVI:** মাল্টিমিডিয়া ভিডিও এবং অডিও ফাইলের স্ট্রিমিং ও রিপ্রেজেন্টেশন স্ট্যান্ডার্ড।
* **GZIP / ZIP:** ডেটার ব্যান্ডউইথ সাশ্রয়ের জন্য ট্রান্সমিশনের পূর্বে সাইজ সংকুচিত করার টেকনোলজি।

### L5: Session Layer (সেশন লেয়ার)
দুটি ডিভাইসের অ্যাপ্লিকেশনের মধ্যে সংযোগ তৈরি, সেশন পরিচালনা ও সমাপ্তি নিয়ন্ত্রণ করে।
* **NetBIOS:** লোকাল নেটওয়ার্কে ডিভাইসগুলোর মধ্যে সেশন তৈরি এবং নাম শেয়ার করার প্রাথমিক সার্ভিস।
* **PPTP:** পয়েন্ট-টু-পয়েন্ট টানেলিং প্রোটোকল, যা মূলত ভিপিএন (VPN) কানেকশন ও সেশন বজায় রাখে।
* **RPC:** রিমোট প্রসিডিউর কল, এক কম্পিউটারের প্রোগ্রাম অন্য কম্পিউটারে সেশন তৈরি করে রান করানোর টেকনোলজি।
* **Sockets (SOCKS):** ক্লায়েন্ট-সার্ভার প্রোটোকল যা প্রক্সি সার্ভারের মাধ্যমে সেশন ট্রাফিক হ্যান্ডেল করে।

### L4: Transport Layer (ট্রান্সপোর্ট লেয়ার)
উৎস থেকে গন্তব্যে ডেটা নিখুঁতভাবে, সিকোয়েন্স অনুযায়ী এবং নির্ভরযোগ্যভাবে পৌঁছানোর দায়িত্ব এই লেয়ারের।
* **TCP:** ট্রান্সমিশন কন্ট্রোল প্রোটোকল। কানেকশন-ওরিয়েন্টেড এবং অত্যন্ত নির্ভরযোগ্য। ডেটা সঠিকভাবে পৌঁছানোর শতভাগ নিশ্চয়তা দেয় (যেমন: ফাইল ডাউনলোড, ওয়েব পেজ লোড)।
* **UDP:** ইউজার ডেটাগ্রাম প্রোটোকল। কানেকশন-লেস এবং অত্যন্ত দ্রুতগতির। ডেটা পৌঁছানোর নিশ্চয়তা না দিলেও রিয়েল-টাইম কমিউনিকেশনে ব্যবহৃত হয় (যেমন: ভিডিও কল, অনলাইন গেমিং)।

### L3: Network Layer (নেটওয়ার্ক লেয়ার)
বিভিন্ন স্বাধীন নেটওয়ার্কের মধ্যে ডেটা প্যাকেট রাউটিং এবং লজিক্যাল অ্যাড্রেসিং বা আইপি হ্যান্ডেল করে।
* **IP (IPv4 / IPv6):** ইন্টারনেট প্রোটোকল, যা গ্লোবাল নেটওয়ার্কে প্রতিটি ডিভাইসের ইউনিক লজিক্যাল ঠিকানা নির্ধারণ করে।
* **ICMP:** ইন্টারনেট কন্ট্রোল মেসেজ প্রোটোকল, নেটওয়ার্কের সমস্যা বা এরর ডায়াগনসিস করার জন্য ব্যবহৃত হয় (যেমন: `ping` কমান্ড)।
* **OSPF / BGP / RIP:** রাউটারগুলোর মধ্যে ডেটা প্যাকেটের সবচেয়ে সেরা এবং দ্রুততম রাস্তা (Routing Path) খুঁজে বের করার প্রোটোকলসমূহ।
* **IPsec:** নেটওয়ার্ক লেয়ার স্তরে ডেটা প্যাকেটগুলোকে এনক্রিপ্ট ও সম্পূর্ণ সুরক্ষিত করার প্রোটোকল সুইট (VPN-এ বহুল ব্যবহৃত)।

### L2: Data Link Layer (ডেটা লিংক লেয়ার)
একই লোকাল নেটওয়ার্কের (LAN) ভেতরের ডিভাইসগুলোর মধ্যে ফিজিক্যাল ম্যাক (MAC) অ্যাড্রেস ব্যবহার করে ফ্রেম আদান-প্রদান করে।
* **Ethernet (802.3):** লোকাল এরিয়া নেটওয়ার্ক বা ল্যান (LAN) তারের মাধ্যমে ডেটা স্থানান্তরের প্রধান ফিজিক্যাল প্রোটোকল।
* **Wi-Fi (802.11):** তারবিহীন বা ওয়ারলেস লোকাল ডেটা স্থানান্তরের বৈশ্বিক স্ট্যান্ডার্ড।
* **ARP:** অ্যাড্রেস রেজোলিউশন প্রোটোকল, যা আইপি অ্যাড্রেস থেকে তার সংশ্লিষ্ট হার্ডওয়্যার ম্যাক (MAC) অ্যাড্রেস খুঁজে বের করে।
* **PPP:** পয়েন্ট-টু-পয়েন্ট প্রোটোকল, সরাসরি যুক্ত দুটি নোড বা ডিভাইসের মধ্যে ডাটা লিঙ্ক তৈরি করে।
* **VLAN (802.1Q):** একটি সিঙ্গেল ফিজিক্যাল সুইচ নেটওয়ার্ককে লজিক্যালি আলাদা আলাদা ভাগে সাব-নেট করার টেকনোলজি।

### L1: Physical Layer (ফিজিক্যাল লেয়ার)
সর্বনিম্ন স্তর, যা কাঁচা বাইনারি বিট (0, 1) সিগন্যালে রূপান্তর করে ক্যাবল বা তরঙ্গের মাধ্যমে ট্রান্সmit করে।
* **1000BASE-T / Ethernet Cables:** কপার ক্যাবলের স্পিড ও মেকানিক্যাল স্ট্যান্ডার্ড (যেমন: Cat5e, Cat6 কেবল)।
* **Fiber Optics (SDH/SONET):** আলোর স্পন্দন ব্যবহার করে ফাইবার গ্লাসের মধ্য দিয়ে অত্যন্ত উচ্চগতির ডেটা স্থানান্তরের ফিজিক্যাল স্ট্যান্ডার্ড।
* **Bluetooth:** কম দূরত্বের মধ্যে ডিভাইসের সাথে ডিভাইসের ওয়ারলেস ডেটা আদান-প্রদানের টেকনোলজি।
* **DSL:** ডিজিটাল সাবস্ক্রাইবার লাইন, ঐতিহ্যবাহী টেলিফোন লাইনের তার ব্যবহার করে ব্রডব্যান্ড ইন্টারনেট সরবরাহের ফিজিক্যাল মাধ্যম।
* **USB / FireWire:** পেরিফেরাল ডিভাইস এবং কম্পিউটারের মধ্যে ফিজিক্যাল কানেকশন ও সিগন্যালিং স্ট্যান্ডার্ড।
